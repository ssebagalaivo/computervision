# Coffee Disease Model Files

Place the trained coffee disease model here.

Expected filename by default:

- `coffee_disease_efficientnetb0.keras`

You can override the path with the `COFFEE_MODEL_PATH` environment variable.
